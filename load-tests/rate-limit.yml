config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 50

scenarios:
  - name: "Rate limit stress"
    flow:
      - post:
          url: "/api/ai/leila"
          json:
            message: "اختبار الضغط"
